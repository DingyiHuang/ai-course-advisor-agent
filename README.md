# AI课程顾问 Agent

OPC“首届软件与智能体开发大赛”的统一赛题作品。项目面向学生/家长、教师、机构/企业三类用户，目标是在严格资料边界内完成身份澄清、需求采集、班型推荐、课程问答、报名引导与多轮会话。

当前版本已完成 TASK-02：项目脚手架、类型化知识、字段来源、上海日期工具和人工金标测试已经建立；推荐与费用规则、模型调用和聊天交互尚未接入。页面仍只用于验证基础运行，不返回课程事实。

## 本地运行

环境要求：Node.js 20.9 或更高版本、npm。

```bash
npm install
cp .env.example .env.local
npm run dev
```

浏览器访问 `http://localhost:3000`。TASK-02 仍不需要真实模型密钥；进入模型集成阶段后，由参赛人在本地 `.env.local` 中填写，前端不会直接读取或调用密钥。

## 质量检查

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

## 项目边界

- 学生课程只使用素材A。
- 教师个人培训只使用素材B。
- 平台、会员和机构服务只使用素材C。
- 确定性代码负责筛选、日期、费用、前置条件、状态与来源拼接。
- LLM负责面向用户的正文和推荐理由措辞，不得用关键词返回整段固定课程答案。
- 原始 Word、素材目录、环境变量和录屏文件不得进入公开仓库。

## 文档

正式交付文档从项目第一阶段持续维护，位于 `docs/`。当前测试结果包含脚手架、知识、来源和上海日期检查；TASK-03的12条规则验收规格已登记为TODO，未执行的业务用例仍保持“待执行”。
