# AI辅助开发记录

## 记录规则

本文件持续记录AI在开发中的实际作用、关键指令、人工决策和人工复核，不能事后虚构。原始资料中的金额、日期、地点和前置条件必须由参赛人通过人工金标复核，不能只相信AI提取结果。

## TASK-01

| 项目 | 记录 |
|---|---|
| AI辅助范围 | 阅读赛题与三份素材；整理不可变开发规范和任务卡；建立Next.js/Vitest脚手架；起草正式交付文档 |
| 关键约束 | 只执行TASK-01；不提前写完整知识或业务规则；不接收或代填API密钥；不代填人工金标 |
| 人工决策 | 类型化实体；不把Playwright放入关键路径；按不可覆盖只正式提交一次；主视频B站、备用GitHub Release；最终仓库public |
| 人工复核 | 参赛人已逐条核对12条事实边界；人工知识金标仍需参赛人独立填写 |
| 结果 | ESLint、TypeScript、Vitest和生产构建通过；首页开发服务器成功启动；Git提交见项目历史 |

后续每阶段追加使用的任务卡、AI主要改动、参赛人纠偏、测试结果和偏离SPEC事项。

## TASK-04

| 项目 | 记录 |
|---|---|
| 使用任务卡 | 完整重读`SPEC.md`、`PROGRESS.md`、`TASK-04_模型与会话.md`及既有规则、来源和测试，只执行TASK-04，不进入TASK-05 |
| 人工前置与决策 | 参赛人自行配置`.env.local`并确认模型额度可维持部署至少7天；明确禁止AI读取、输出、记录或提交密钥；要求模型闸门任一失败立即停止 |
| 真实模型闸门 | AI只检查文件存在与Git忽略规则。余额问题由参赛人处理后重新执行：脱敏模型`deepseek-v4-flash`普通中文HTTP 200/246ms；严格JSON HTTP 200/851ms，可直接`JSON.parse`；两项均低于20000ms。未记录密钥、请求头、模型地址或完整配置 |
| AI辅助实现 | 建立服务端模型适配、严格JSON解析、classifier、composer、会话状态与编排、事实计划、程序来源尾注、高风险金额/日期校验、一次重试和可注入下一次失败机制；复用TASK-03规则与字段来源，没有重写业务规则 |
| 红线落实 | classifier温度0，composer温度0.6；所有课程正文和理由由composer动态生成；模型不能写素材名或章节；程序校验`decisionTrace.constraintKeys`、`usedFactIds`及金额日期后追加来源；失败不返回固定课程答案 |
| 关键人工约束 | 广州学生先说明素材A无广州线下班，再问北京、上海或均不便；零基础教师落为`startingLevel=beginner`且不虚构`goal`；`nextQuestionKeys`优先于状态；用户上下文在失败后保持不变 |
| 测试区分 | 两次闸门为真实供应商调用；其余TASK-04单元测试全部使用测试替身。全量13个测试文件、74条测试通过，不把测试替身记作生产调用，也不把本阶段单测冒充官方25组 |
| 偏离与遗留 | 未偏离TASK-04的职责边界。TASK-05界面、`?test=1`控件、对话导出和人工视频链路尚未实施；官方25组留待TASK-06 |
