# 技术说明

## 架构原则

项目使用 Next.js App Router、TypeScript 和服务端 Serverless 接口。核心设计是把确定性决策与语言表达分离：代码负责资料域路由、班型筛选、日期冲突、费用和优惠计算、前置条件、状态流转、异常拦截与来源拼接；LLM只负责基于已确认结构化事实生成面向用户的正文和推荐理由。不得用关键词直接返回整段固定课程答案。

知识层采用 `Camp`、`TeacherProduct`、`PlatformService` 等类型化实体。字段级来源映射生成事实编号，模型只返回本次使用的 `usedFactIds`，程序校验后统一追加资料名称和章节，避免模型编造引用。业务日期统一转为 `Asia/Shanghai` 后比较，并支持固定时间注入。费用和推荐结果由规则层输出，composer温度计划设为约0.6以保留措辞变化；classifier温度为0以提高稳定性。

模型配置只存在于服务端环境变量 `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`、`LLM_TIMEOUT_MS`。真实密钥不进入前端、仓库、日志、文档或对话导出。`.gitignore`还排除原始Word、素材目录和录屏文件，最终公开仓库只包含二次加工的结构化运行数据。

## 确定性规则层

TASK-03在`src/lib/rules`建立纯TypeScript业务层，不依赖网络或模型：

- `status.ts`分别计算报名截止和早鸟截止，截止当日仍为有效，并在比较前校验`YYYY-MM-DD`业务日期。教师早鸟“开课日前14日”的口径统一解释为“截止日＝开课日－14个自然日”，截止当日24:00前仍有效；例如8月1日开课对应7月18日截止。
- `feeCalculator.ts`计算学生和教师费用。学生团报资格读取`Camp.groupMinimum`、`groupDiscount`、`groupScope`，由调用方传入是否满足同一期同一班型；早鸟与团报只采用优惠额较大的一项。食宿仅允许线下班并在优惠后加价。
- `studentRecommendation.ts`根据地域、营期、出行、形式和回放需求筛选真实组合，最多返回2项；不足两个有效约束时继续询问，用户拒绝或连续两轮无新增约束时返回可退出的资料不足状态。广州家长首次要求本地线下时返回`boundary_follow_up`并追问`canTravel`，不能出行后才转线上，能出行则继续确认北京或上海目的地。
- `teacherRecommendation.ts`把明确目标或起始能力映射到L1、L2、L3，把连续时间映射为集训、不能脱岗映射为周末。“零基础”使用已采集的`startingLevel=beginner`映射L1，不虚构学习目标。未说明时间形式时不会默认集训；L2/L3前置未满足时返回阻断和下一步，前置未知时先询问。
- `institutionRouting.ts`仅返回素材C的机构服务并标记不进入个人推荐；`boundaries.ts`把教师退款未提供、会员价格未提供建模为结构化边界。

推荐项携带`decisionTrace`，每一项包含规则代码、参与判断的用户约束键、对应`constraintValues`和实际使用的事实编号。所有`constraintKeys`在进入composer前必须是本轮已采集键的子集；带`nextQuestionKeys`的结果优先进入追问路由，不能只按状态生成终止回答。学生推荐进入composer前还会把最终确认状态重新交给纯规则推荐器，要求canonical实体集合、营期和trace完全一致；任何同营期但校区、授课形式或出行条件不一致的实体也会被拒绝。费用决策记录报名、早鸟和优惠分支。`src/lib/citations`只根据这些事实编号读取字段来源表，按资料、章节和小节去重；它不生成课程正文，也不允许模型提供章节号。

## 服务端模型适配与实测结论

TASK-04建立了可替换的OpenAI兼容服务端适配层。`src/lib/llm/runtime.ts`使用`server-only`保护，只在请求运行时读取已命名的环境变量；前端只能请求`POST /api/chat`，不能获得模型地址、模型名或密钥。客户端统一处理超时、HTTP错误、响应信封和文本提取，不记录请求头、密钥或完整敏感配置。

2026-07-22重新执行真实模型闸门，脱敏模型名为`deepseek-v4-flash`：普通中文请求HTTP 200，端到端耗时246ms；启用`response_format: { type: "json_object" }`的严格JSON请求HTTP 200，耗时851ms，响应无Markdown代码块或前后缀，可直接`JSON.parse`。两次均显著低于`LLM_TIMEOUT_MS=20000`，因此本阶段保留20秒超时；供应商当前兼容`response_format`/JSON mode。若供应商以后取消兼容，系统会把非法JSON作为模型错误处理并最多重试一次，不会用字符串截取或代码块清洗伪装成严格结构化输出。

classifier固定使用`temperature=0`和JSON mode，只返回身份、意图、资料主题、实体指代及约束候选。候选结构只接受SPEC定义的键和值；程序再独立识别用户原文中的地区、第一/二/三期、线上/线下和出行规范表达。显式闭集值与classifier或旧状态冲突时，代码值优先并记录脱敏`correction`，例如“朝阳区”统一为`beijing`、“第一期”不能被候选`[2]`覆盖、“周末可以上课”不能写成营期。composer使用`temperature=0.6`和JSON mode；推荐路由只接收最终确认约束、最终实体、程序计算和`decisionTrace`，不再接收可能污染推荐的旧自由文本。它负责动态生成正文和理由，但不得生成素材名或章节；程序根据经过白名单校验的`usedFactIds`追加完整来源尾注。

## 会话接口与状态合同

`POST /api/chat`接收`message`、上一次返回的`state`、可选`action`、稳定`clientRequestId`、可选`retryOf`和测试模式标记。`action`支持`message`、`reset`、`menu`、`catalog`、`select_domain`、`select_entity`和`inject_next_failure`。显式身份和班型选择由代码校验，不需要解析AI正文；`catalog`由程序准备当前资料域的全部事实后仍交给composer动态组织；最后一项只有开发测试模式才可设置一次故障，生产Route会强制清除客户端传入的测试标志。响应包含`status`、最终正文`message`、新`state`、程序收集的`sources`、相关`entityIds`、允许的`actions`、结构化`presentation`和程序生成的`notices`；边界流程另返回`boundaryCode`，失败时另含脱敏的`error.code`与`retryable`。只有非生产环境且请求显式`diagnostics=true`时才返回classifier闭集候选、代码纠正、确认状态、待追问键、canonical实体、trace、grounding原因码、composer次数和耗时；不返回证据原文、模型原始输出、请求头、密钥或完整提示词。

服务端状态保存当前资料域、学生/教师约束、机构需求、当前班型、最近推荐、待追问键与选项、短上下文以及单次测试故障标志。所有传入状态都先清洗；重置或返回菜单会清空身份、约束、机构分类、当前实体、最近推荐、待追问和故障状态。页面可以保留只读历史消息，但这些历史不再作为新业务状态参与推荐；返回菜单后再输入“我想学AI”必须重新澄清身份。跨域切换作为一次原子状态转换：保留页面对话历史，先清空旧域业务状态，再从本条消息采集新域约束。API以`identity_switched`通知返回切换前后域和可见系统文案，前端单独显示系统消息。推荐后只有唯一候选时才继承“这个班”；有两个候选时继续追问选择，不擅自绑定其中一个。

TASK-05第二轮热修在`src/lib/conversation/routing.ts`把确定性闭集归一从“只处理待追问字段”提升为每条学生消息的独立覆盖层。顺序固定为：明确机构服务语境、明确第一人称个人身份、显式地区/营期/授课形式/出行规范值、当前实体事实问法、LLM classifier、无关问题兜底。机构采购信号优先于“教师”等个人身份词；孤立“学校”不会触发切换。同一句“我是广州家长，第一期只想线下”不再因身份早退而漏采其余约束；“均不便出行”在classifier之前映射到`canTravel=false`；“我住朝阳区”归一为北京；“改成线上”等显式修改会覆盖旧值。该层只决定身份、状态字段和操作类型，不按关键词返回课程正文。

生成前依次执行以下保护：带`nextQuestionKeys`的计划无条件优先走追问路由；待追问键必须在各资料域白名单内；每个`decisionTrace`的键和值必须与最终确认状态一致；推荐实体必须与重新执行的确定性推荐完全相同；`usedFactIds`必须来自本次计划事实；composer不得输出来源标记或新增动作；金额与日期必须来自实际使用的事实、用户原文或程序计算。grounding失败使用机器原因码，例如`ungrounded_amount`、`invalid_fact_id`、`missing_required_fact`、`external_commitment`和`recommendation_reason_mismatch`。学校采购正文必须实际包含“20人起、5万元起”并使用对应事实ID。推荐理由不得追问区县、学校、周末/晚间、考级或把录播当授课形式；机构回答不得声称已安排顾问、稍后联系、已提交需求、已锁位或已报名。

金额校验使用`src/lib/validation/money.ts`的受控归一化：只识别带`元`、`千元`、`万元`或人民币符号的金额语境，支持`5万元`、`五万元`、`1万—3万元`和`500—1500元/人`等表达并转换为基础元值。允许金额只从价格/费用字段、程序计算、明确金额文本和用户原文收集；`minimumPeople`、回放天数、课程日序等普通数字不再被当作金额。日期校验仍只识别ISO日期、月日和明确月日范围，不扩展为数字白名单。因此“20人起”“第5天”“30天回放”“1—2个班型”放行，未授权“2万元起”仍被拒绝。

广州学生线下边界由规则层先返回`boundary_follow_up`。程序固定追加“素材A没有广州学生线下班。”这一事实边界，再要求composer询问能否去北京、上海或均不便出行；只有用户明确均不便出行后才允许推荐线上。这里的固定前缀属于资料边界声明，不是固定课程答案，课程说明和推荐理由仍由composer动态生成。

模型超时、5xx、限流、非法JSON或生成校验失败最多重试一次，第一次失败不会形成API错误响应。第二稿会收到固定原因码映射的脱敏纠偏指令；两次仍失败才返回一次可重试错误并保留调用前状态。广州降级的第二稿理由由程序按已确认`decisionTrace`归一为营期、广州无学生线下、京沪均不便、线上为降级备选四项，并只补计划内`replayDays`事实；正文仍由composer生成，未知事实、金额、日期和外部承诺仍会被拒绝。测试故障存放在会话状态的`test.failNextModelCall`，开发模式消费一次后清除；生产Route在信任边界强制关闭该标志。

## TASK-05界面、推荐展示与导出

`src/components/CourseAdvisor.tsx`是交互边界，负责页面消息、输入校验、加载状态、结构化动作、重试快照、测试模式显示和浏览器下载；它不包含课程知识或模型配置。`src/app/page.tsx`在服务端读取`searchParams`，只把`test=1`布尔值传给客户端组件，因此普通URL不会先渲染再闪现测试按钮。CSS Module实现桌面双栏和手机单列布局，用户、AI、系统与错误使用独立语义样式。

`src/lib/conversation/presentation.ts`把已确定的知识实体、费用计算、LLM动态理由和程序来源组合为`RecommendationCard`或`InstitutionServiceCard`。产品名称、日期、地点、标准价和实际价来自代码；推荐理由来自composer，约束标签和值来自当前已清洗状态；卡片来源只保留该实体实际使用的事实。程序固定显示“资料未提供实时余位”，不会把`capacity`或`minimumToOpen`当作剩余名额。机构服务卡只读取`PlatformService`，不会加载学生或教师个人价格。

前端不扫描AI正文决定按钮。`pendingQuestionOptions`直接渲染为回答选项，`actions`按服务端允许的精确动作名映射；“继续咨询该班”调用`select_entity`并且服务端只接受`lastRecommendationIds`中的实体。“查看全部课程”调用`catalog`，不会依赖“全部课程”等关键词分类。

每次发送生成稳定`clientRequestId`。每条错误消息保存`clientRequestId`、原`message`、原`state`、原`action`和`errorMessageId`；重试沿用原ID并设置`retryOf`，强制`testMode=false`并清除模拟故障位。客户端reducer按请求ID原位更新：同一请求只保留一条用户消息、一张错误卡或一条成功AI消息，推荐卡按“请求ID＋canonical实体ID”去重；同步请求租约阻止双击产生第二个fetch，同一`errorMessageId`完成后不能再次消费。处理一张错误卡不会清除其他错误的快照；返回菜单会移除可见错误卡并清空全部重试状态。空输入和超过500字在`fetch`前拦截；网络异常只生成本地脱敏错误卡。

`src/lib/export/markdown.ts`是纯数据导出模块。它从当前实际消息、`ConversationState`、结构化推荐和来源生成Markdown，并用`Intl.DateTimeFormat`显式指定`Asia/Shanghai`。实际对话保持完整；“推荐结果”只汇总当前身份资料域的卡片，并按canonical entity ID去重，同一产品多次回答只保留一条。来源区保留完整会话实际使用来源并明确标注“完整会话历史”。文件包含测试编号、身份、约束、当前班型/机构分类、实际对话、推荐、来源、测试模式和实际错误；没有消息时明确输出“尚无”，不构造示例对话。浏览器下载文件名带会话编号和上海时间，单元测试使用固定时间验证内容与文件名。

## 日期、费用和职责边界复述

教师早鸟“开课日前14日”的业务定义固定为“截止日＝开课日－14个自然日，截止当日24:00前有效”。例如8月1日开课对应7月18日截止。Serverless运行时的UTC时间不能直接参与判断，必须先转换为`Asia/Shanghai`业务日期并按`currentDate <= deadline`比较。

代码负责身份显式动作、约束清洗、资料域、筛选、日期、费用、优惠、前置条件、推荐映射、会话状态、异常拦截和来源追加。classifier只提取带用户原文证据的候选；composer只根据本次事实、计算和决策轨迹动态生成正文与推荐理由。页面只展示结构化响应。三层均不得用关键词直接返回整段固定课程答案。

## 当前实现状态

项目当前包含9个学生营期/班型组合、6个教师产品和7项平台服务。业务日期统一使用`Asia/Shanghai`，支持固定时钟且UTC跨日边界测试继续通过。参赛人亲自提供的人工知识金标59项断言，以及规则层7个费用结果、10个状态结果，均作为独立预期参与测试。

提交`f0f6870f44112d09851089d42b2582577f0b055e`现定义为TASK-05检查点：当时16个测试文件、141条自动测试通过，但参赛人随后真实浏览器复验总体失败，不能作为v1最终验收。第二轮热修当前停留在未提交工作区，状态为“代码已修复、待参赛人再次人工复验”。

当前Mock Route集成为`tests/integration/api-chat.spec.ts`的34条；全量为17个测试文件、178条通过。另以真实环境变量启动应用并真实调用`POST /api/chat`：学校20人采购3/3、广州第一期均不便出行3/3、北京第一期线下3/3均达到本轮闸门；返回菜单和人工承诺边界也完成真实Route补充验证。Mock测试、真实模型Route重放和参赛人浏览器复验是三个独立证据层：最后一层仍保持第3项通过、第4项部分通过、第5项失败、第6项部分通过，必须由参赛人重新复验后才能改变。官方25组仍未执行，本任务不进入TASK-06、不部署、不提交Git。
